version: 2

sources:
  - name: raw
    database: terraform-demo-457908
    schema: Nvo_taxi
    tables:
      - name: raw_yellow
        identifier: yellow_tripdata_external_table
      - name: raw_green
        identifier: green_tripdata_external_table
      - name: raw_fhv
        identifier: fhv

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
